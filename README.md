# Purpose
- Hosts a static website for the artifacts enhanced during my capstone at Southern New Hampshire University.
# Build
- Assuming you've cloned the project, run `npm install` from the root of the project to install necessary dependencies.
- Then `npm run build`
# Start a local server
- `npm run dev`
